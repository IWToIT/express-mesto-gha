const NOT_FOUND = 'NotFound';
const CAST_ERROR = 'CastError';

const badRequest = 400;
const notFound = 404;
const defaultErr = 500;

module.exports = {
  NOT_FOUND,
  CAST_ERROR,
  defaultErr,
  badRequest,
  notFound,
};
